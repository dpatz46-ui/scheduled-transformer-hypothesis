# Data

Large activation tensors are intentionally excluded from the Git repository.

Future archival releases should provide data through an appropriate large-file or research-data repository and record checksums, model/checkpoint provenance, extraction-code version, and schema documentation here.

Do not commit raw HDF5 activation harvests directly to ordinary Git history.
